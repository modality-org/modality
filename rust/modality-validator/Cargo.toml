[package]
name = "modality-validator"
version = "0.1.0"
edition = "2021"
description = "Blockchain validator for Modality - observes mining chain without mining"
license = "MIT"
repository = "https://github.com/modality-org/modality"
homepage = "https://www.modality.org"
documentation = "https://docs.rs/modality"

[dependencies]
anyhow = "1.0"
thiserror = "1.0"
log = "0.4"
tokio = { version = "1", features = ["rt", "sync", "time", "macros"] }
modality-observer = { path = "../modality-observer", version = "0.1.0" }
modality-datastore = { path = "../modality-datastore", version = "0.1.0" }
modality-validator-consensus = { path = "../modality-validator-consensus", version = "0.1.0" }
modality-wasm-runtime = { path = "../modality-wasm-runtime", version = "0.1.0" }
modality-wasm-validation = { path = "../modality-wasm-validation", version = "0.1.0" }
modality-lang = { path = "../modality-lang", version = "0.1.7" }
wasmtime = "26.0"
libp2p-identity = { version = "0.2.12", features = ["serde"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
base64 = "0.22"

[dev-dependencies]
tokio = { version = "1", features = ["rt", "macros", "test-util"] }
env_logger = "0.11"
tempfile = "3.5"

[features]
default = []
persistence = ["modality-validator-consensus/persistence"]