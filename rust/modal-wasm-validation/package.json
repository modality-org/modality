{
  "name": "modal-wasm-validation",
  "version": "0.1.0",
  "description": "Built-in validation logic for Modality contracts (WASM)",
  "main": "dist/modal_wasm_validation.js",
  "types": "dist/modal_wasm_validation.d.ts",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "wasm-pack build --target web --out-dir dist",
    "build-node": "wasm-pack build --target nodejs --out-dir dist-node",
    "build-bundler": "wasm-pack build --target bundler --out-dir dist-bundler",
    "test": "wasm-pack test --headless --firefox",
    "clean": "rm -rf dist dist-node dist-bundler"
  },
  "keywords": [
    "modality",
    "validation",
    "wasm",
    "webassembly",
    "deterministic"
  ],
  "author": "Modality Team",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/modality-org/modality"
  },
  "devDependencies": {
    "wasm-pack": "^0.12.1"
  }
}

