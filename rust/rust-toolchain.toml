# aws-smithy-types 1.4.9 (pinned in modal-networks) requires rustc 1.91.1.
# cross mounts this host toolchain into the Linux build container.
[toolchain]
channel = "1.91.1"
targets = ["x86_64-unknown-linux-gnu", "wasm32-unknown-unknown"]
