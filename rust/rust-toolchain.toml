# The locked workspace dependency graph requires rustc 1.94.1 or newer.
# Keep this pinned so source-build onboarding and cross builds use the same compiler floor.
[toolchain]
channel = "1.94.1"
targets = ["x86_64-unknown-linux-gnu", "wasm32-unknown-unknown"]
