# get.modal.money (redirect)

Package distribution moved to [get.modality.org](https://get.modality.org).
This stack keeps `get.modal.money` as a path-preserving 301 so old install
commands continue to work.

```bash
curl -fsSL https://get.modality.org/testnet/latest/install.sh | sh
```

Build/registry scripts live in `sites/get.modality.org/`.
