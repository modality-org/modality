# Modal.money landing (redirect)

The network homepage now lives at [modality.network](https://modality.network).
This directory is a redirect page so `modal.money` and `www.modal.money`
send visitors there.

Infrastructure: `sites/www.modal.money/` (CloudFront 301 + this HTML fallback).
